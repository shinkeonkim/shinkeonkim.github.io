---
title: "Suffix Array (접미사 배열)"
aliases: ["suffix array", "접미사 배열", "SA", "LCP array"]
tags: [algorithm, string, suffix-array, lcp]
updated: 2026-06-25
references:
  - id: solved-ac
    note: "tag: suffix_array"
  - title: "Manber & Myers, Suffix Arrays (1990)"
    url: "https://doi.org/10.1137/S0097539797328707"
    author: "Manber & Myers"
---

## 정의

**Suffix Array (SA, 접미사 배열)** 는 문자열 s 의 **모든 접미사를 사전순 정렬한 인덱스 배열**. SA[i] = 시작 위치 (0-indexed). 보통 **LCP (Longest Common Prefix) 배열** 을 함께 구축.

```text
s = "banana"
suffixes:
  0: banana      정렬: 5:a, 3:ana, 1:anana, 0:banana, 4:na, 2:nana
SA = [5, 3, 1, 0, 4, 2]
LCP = [0, 1, 3, 0, 0, 2]  // lcp between adjacent suffixes
```

## 문제 상황과 동기

문자열의 **부분문자열 검색, 반복 패턴, 공통 접두사** 등의 문제가 매일 등장.

- **naive**: 부분문자열 검색 O(NM). 모든 접미사 정렬 O(N² log N).
- **Suffix Array**: O(N log N) 구축 후, 검색 O(M log N) (binary search on SA). LCP 배열로 **서로 다른 부분문자열 개수, k 번째 부분문자열, 가장 긴 반복 부분문자열**을 O(N) 에 계산.

핵심 통찰: *접미사들을 정렬하면, 문자열의 모든 부분문자열이 접두사로 드러난다.*

## 시각화

```anim:suffix-array
{}
```

## 핵심 아이디어

**Doubling (Manber-Myers)**: 각 단계에서 접미사를 `(rank[i], rank[i+k])` 쌍으로 정렬, k 를 2 배씩 증가. O(N log N).

**LCP (Kasai)**: `k = 0; for(i) { if(rank[i]==0) continue; j=SA[rank[i]-1]; while(s[i+k]==s[j+k]) k++; LCP[rank[i]]=k; if(k) k--; }`. O(N).

## 알고리즘

```text
build_sa(s):
    n = len(s)
    초기 rank = s[i] 의 ASCII 값
    k = 1
    while k < n:
        각 접미사의 키 = (rank[i], rank[i + k] or -1)
        std::sort 로 정렬
        새 rank 부여 (같으면 같은 rank)
        k *= 2
    return SA

build_lcp(s, sa):
    rank[sa[i]] = i
    k = 0
    for i in 0..n-1:
        if rank[i] == 0: continue
        j = sa[rank[i] - 1]
        while s[i+k] == s[j+k]: k++
        lcp[rank[i]] = k
        if k > 0: k--
    return LCP
```

## 구현

<CodeWithOutput
  variants={[
    {
      language: "cpp",
      label: "C++",
      code: `// Suffix Array (doubling) + LCP (Kasai)
#include <bits/stdc++.h>
using namespace std;

vector<int> build_sa(const string& s) {
    int n = s.size();
    vector<int> sa(n), rank(n), tmp(n);
    for (int i = 0; i < n; i++) { sa[i] = i; rank[i] = s[i]; }
    for (int k = 1; k < n; k *= 2) {
        auto cmp = [&](int a, int b) {
            if (rank[a] != rank[b]) return rank[a] < rank[b];
            return (a + k < n ? rank[a + k] : -1) < (b + k < n ? rank[b + k] : -1);
        };
        sort(sa.begin(), sa.end(), cmp);
        tmp[sa[0]] = 0;
        for (int i = 1; i < n; i++) tmp[sa[i]] = tmp[sa[i-1]] + cmp(sa[i-1], sa[i]);
        rank = tmp;
    }
    return sa;
}

vector<int> build_lcp(const string& s, const vector<int>& sa) {
    int n = s.size();
    vector<int> rank(n), lcp(n);
    for (int i = 0; i < n; i++) rank[sa[i]] = i;
    int k = 0;
    for (int i = 0; i < n; i++) {
        if (rank[i] == 0) continue;
        int j = sa[rank[i] - 1];
        while (i + k < n && j + k < n && s[i+k] == s[j+k]) k++;
        lcp[rank[i]] = k;
        if (k) k--;
    }
    return lcp;
}

int main() {
    string s; cin >> s;
    auto sa = build_sa(s);
    auto lcp = build_lcp(s, sa);
    for (int i : sa) cout << i << " ";
    cout << "\n";
    for (int i : lcp) cout << i << " ";
}`,
    },
    {
      language: "python",
      label: "Python",
      code: `def build_sa(s):
    n = len(s)
    sa = list(range(n))
    rank = [ord(c) for c in s]
    tmp = [0] * n
    k = 1
    while k < n:
        sa.sort(key=lambda x: (rank[x], rank[x + k] if x + k < n else -1))
        tmp[sa[0]] = 0
        for i in range(1, n):
            prev, cur = sa[i-1], sa[i]
            pk = rank[prev + k] if prev + k < n else -1
            ck = rank[cur + k] if cur + k < n else -1
            tmp[cur] = tmp[prev] + (rank[prev] != rank[cur] or pk != ck)
        rank = tmp
        k <<= 1
    return sa

def build_lcp(s, sa):
    n = len(s)
    rank = [0] * n
    for i, pos in enumerate(sa): rank[pos] = i
    lcp, k = [0] * n, 0
    for i in range(n):
        if rank[i] == 0: continue
        j = sa[rank[i] - 1]
        while i + k < n and j + k < n and s[i+k] == s[j+k]: k += 1
        lcp[rank[i]] = k
        if k: k -= 1
    return lcp

s = input().strip()
sa = build_sa(s)
lcp = build_lcp(s, sa)
print(*sa)
print(*lcp)`,
    },
    {
      language: "java",
      label: "Java",
      code: `import java.util.*;
import java.io.*;
public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String s = br.readLine();
        int n = s.length();
        Integer[] sa = new Integer[n];
        int[] rank = new int[n], tmp = new int[n];
        for (int i = 0; i < n; i++) { sa[i] = i; rank[i] = s.charAt(i); }
        for (int k = 1; k < n; k *= 2) {
            final int K = k;
            final int[] R = rank;
            Arrays.sort(sa, (a, b) -> {
                if (R[a] != R[b]) return Integer.compare(R[a], R[b]);
                return Integer.compare(a+K<n?R[a+K]:-1, b+K<n?R[b+K]:-1);
            });
            tmp[sa[0]] = 0;
            for (int i = 1; i < n; i++) {
                int a = sa[i-1], b = sa[i];
                boolean diff = R[a] != R[b] || (a+K<n?R[a+K]:-1) != (b+K<n?R[b+K]:-1);
                tmp[b] = tmp[a] + (diff ? 1 : 0);
            }
            rank = tmp;
        }
        StringBuilder sb = new StringBuilder();
        for (int x : sa) sb.append(x).append(" ");
        System.out.println(sb);
    }
}`,
    },
  ]}
  cases={[
    {
      label: "banana",
      input: `banana`,
      output: `5 3 1 0 4 2
0 1 3 0 0 2`,
    },
    {
      label: "ababa",
      input: `ababa`,
      output: `4 2 0 3 1
0 1 3 0 1`,
    },
  ]}
/>

## 복잡도

| 항목 | 값 |
|:---|:---|
| **SA 구축 (doubling)** | O(N log N) 시간, O(N) 공간 |
| **SA 구축 (SA-IS)** | O(N) 시간, O(N) 공간 |
| **LCP 구축 (Kasai)** | O(N) 시간, O(N) 공간 |
| **부분문자열 검색** | O(M log N) (binary search on SA) |
| **서로 다른 부분문자열 수** | O(N) (n*(n+1)/2 - Σ LCP) |

## 활용

| 응용 | 방법 | 복잡도 |
|:---|:---|---:|
| **서로 다른 부분문자열 수** | `n(n+1)/2 - sum(LCP)` | O(N) |
| **k 번째 부분문자열** | LCP 누적, k 추적 | O(N) |
| **최장 반복 부분문자열** | LCP argmax | O(N) |
| **두 문자열 LCS** | `S+$+T+#` 의 SA+LCP | O(S+T) |
| **최소 회전** | `s+s` 의 SA 에서 첫 N 이내 | O(N log N) |

## 함정

### 1. 0-indexed vs 1-indexed

SA 원소는 시작 위치. LCP[0] 은 정의가 없음 (보통 0). 일관성 유지.

### 2. 정렬 비교 함수

Doubling 에서 `(rank[i], rank[i+k])` 쌍이 잘못 들어가면 순서가 꼬임. i+k 가 범위 밖일 때 -1 처리.

### 3. LCP 와 SA 순서

LCP[i] = s[SA[i-1]] 과 s[SA[i]] 의 공통 접두사 길이 (i >= 1). i=0 은 0.

### 4. 메모리

SA + LCP + rank = 3× N ints. N = 5×10^5 까지는 OK, 10^6 이상이면 메모리 주의.

## BOJ 연습 문제

| 번호 | 제목 | 정답률 | 링크 |
|:---|:---|---:|:---|
| BOJ 9248 | Suffix Array | - | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/9200-9299/9248) |
| BOJ 11479 | 서로 다른 부분 문자열의 개수 2 | - | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/11400-11499/11479) |
| BOJ 11656 | 접미사 배열 | - | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/11600-11699/11656) |
| BOJ 13264 | 접미사 배열 2 | - | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/13200-13299/13264) |
| BOJ 1605 | 가장 긴 반복 부분문자열 | - | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/1600-1699/1605) |
| BOJ 18979 | 3주차 포스터 | - | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/18900-18999/18979) |

## 참고

- [[Suffix Automaton|접미사 오토마타]]
- [[Prefix Sum|누적 합]]
- [[Z Algorithm|Z 알고리즘]]
- [[KMP|KMP]]
