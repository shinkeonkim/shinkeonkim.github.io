---
title: "격자 그래프 (Grid Graph)"
aliases: ["grid graph", "격자 그래프", "grid bfs", "grid dfs"]
tags: [algorithm, graph, grid, bfs, dfs]
updated: 2026-06-24
references:
  - id: solved-ac
    note: "tag: graph"
  - id: cp-algorithms
    note: "graph/breadth-first-searc"
---

## 정의

**격자 그래프 (Grid Graph)** 는 **2차원 배열 (N × M 격자) 위의 그래프**. 각 칸이 노드, 인접한 칸 사이가 간선. 4방향 또는 8방향 이동.

격자 위 최단 경로, 연결 영역, 탐색은 BFS/DFS 의 정형 패턴. PS 에서 매우 빈번.

## 문제 상황과 동기

2D 맵 / 미로 / 게임 보드에서 탐색 / 최단 경로.

- **일반 그래프**: 인접 리스트 `adj[u] = [v1, v2, ...]`.
- **격자 그래프**: 2차원 배열 `grid[x][y]`. 인접 = 4방향 (상하좌우) 또는 8방향 (대각선 포함).

격자의 장점:

- 인접 관계가 암묵적 (explicit edge list 불필요).
- 좌표 (x, y) 로 직관적.
- 캐시 지역성 좋음.

핵심 통찰: 격자는 *암묵적 그래프 (implicit graph)*. 간선을 저장하지 않고 좌표 연산 `(x±1, y±1)` 으로 인접 노드 생성.

응용:

- 미로 최단 경로 (BFS)
- 섬 개수 세기 (DFS)
- 지뢰 찾기, 체스, 게임 AI

## 시각화

```anim:grid-graph
{}
```

## 핵심 아이디어

invariant: *칸 (x, y) 의 인접 칸은 (x+dx, y+dy), dx/dy ∈ 방향 집합*.

### 4방향 (상하좌우)

```text
dx = [-1, 1,  0, 0]
dy = [ 0, 0, -1, 1]

neighbors(x, y) = [(x+dx[i], y+dy[i]) for i in 0..3 if valid]
```

### 8방향 (대각선 포함)

```text
dx = [-1, -1, -1,  0, 0,  1, 1, 1]
dy = [-1,  0,  1, -1, 1, -1, 0, 1]
```

### 경계 체크

```text
valid(x, y) = (0 <= x < N) and (0 <= y < M) and (grid[x][y] ≠ wall)
```

## 알고리즘

### BFS (최단 경로)

```text
grid_bfs(grid, sx, sy, tx, ty):
    queue q
    dist[sx][sy] = 0
    q.push((sx, sy))
    
    while q not empty:
        (x, y) = q.pop()
        
        if x == tx and y == ty:
            return dist[x][y]
        
        for (dx, dy) in directions:
            nx = x + dx, ny = y + dy
            if valid(nx, ny) and not visited[nx][ny]:
                visited[nx][ny] = true
                dist[nx][ny] = dist[x][y] + 1
                q.push((nx, ny))
    
    return -1  # unreachable
```

### DFS (연결 영역)

```text
grid_dfs(x, y):
    if not valid(x, y) or visited[x][y]:
        return
    
    visited[x][y] = true
    area_size++
    
    for (dx, dy) in directions:
        grid_dfs(x + dx, y + dy)
```

## 구현

<CodeWithOutput
  variants={[
    {
      language: "cpp",
      label: "C++",
      code: `// 격자 그래프 BFS, 최단 거리
#include <bits/stdc++.h>
using namespace std;

int dx[] = {-1, 1, 0, 0};
int dy[] = {0, 0, -1, 1};

int grid_bfs(vector<string>& grid, int sx, int sy, int tx, int ty) {
    int n = grid.size(), m = grid[0].size();
    vector<vector<int>> dist(n, vector<int>(m, -1));
    queue<pair<int, int>> q;
    
    q.push({sx, sy});
    dist[sx][sy] = 0;
    
    while (!q.empty()) {
        auto [x, y] = q.front(); q.pop();
        
        if (x == tx && y == ty)
            return dist[x][y];
        
        for (int i = 0; i < 4; i++) {
            int nx = x + dx[i], ny = y + dy[i];
            if (nx >= 0 && nx < n && ny >= 0 && ny < m &&
                grid[nx][ny] != '#' && dist[nx][ny] == -1) {
                dist[nx][ny] = dist[x][y] + 1;
                q.push({nx, ny});
            }
        }
    }
    return -1;
}

int main() {
    int n, m, sx, sy, tx, ty;
    cin >> n >> m >> sx >> sy >> tx >> ty;
    
    vector<string> grid(n);
    for (auto& s : grid) cin >> s;
    
    cout << grid_bfs(grid, sx, sy, tx, ty) << "\\n";
}`,
    },
    {
      language: "python",
      label: "Python",
      code: `# 격자 그래프 BFS, collections.deque
from collections import deque
import sys
input = sys.stdin.readline

def grid_bfs(grid, sx, sy, tx, ty):
    n, m = len(grid), len(grid[0])
    dist = [[-1] * m for _ in range(n)]
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]
    
    q = deque([(sx, sy)])
    dist[sx][sy] = 0
    
    while q:
        x, y = q.popleft()
        
        if x == tx and y == ty:
            return dist[x][y]
        
        for dx, dy in directions:
            nx, ny = x + dx, y + dy
            if 0 <= nx < n and 0 <= ny < m and grid[nx][ny] != '#' and dist[nx][ny] == -1:
                dist[nx][ny] = dist[x][y] + 1
                q.append((nx, ny))
    
    return -1

n, m, sx, sy, tx, ty = map(int, input().split())
grid = [input().strip() for _ in range(n)]

print(grid_bfs(grid, sx, sy, tx, ty))`,
    },
    {
      language: "java",
      label: "Java",
      code: `// 격자 그래프 BFS, Queue<int[]>
import java.util.*;
import java.io.*;

public class Main {
    static int[] dx = {-1, 1, 0, 0};
    static int[] dy = {0, 0, -1, 1};
    
    static int gridBFS(String[] grid, int sx, int sy, int tx, int ty) {
        int n = grid.length, m = grid[0].length();
        int[][] dist = new int[n][m];
        for (int[] row : dist) Arrays.fill(row, -1);
        
        Queue<int[]> q = new LinkedList<>();
        q.add(new int[]{sx, sy});
        dist[sx][sy] = 0;
        
        while (!q.isEmpty()) {
            int[] cur = q.poll();
            int x = cur[0], y = cur[1];
            
            if (x == tx && y == ty)
                return dist[x][y];
            
            for (int i = 0; i < 4; i++) {
                int nx = x + dx[i], ny = y + dy[i];
                if (nx >= 0 && nx < n && ny >= 0 && ny < m &&
                    grid[nx].charAt(ny) != '#' && dist[nx][ny] == -1) {
                    dist[nx][ny] = dist[x][y] + 1;
                    q.add(new int[]{nx, ny});
                }
            }
        }
        return -1;
    }
    
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(br.readLine());
        int n = Integer.parseInt(st.nextToken());
        int m = Integer.parseInt(st.nextToken());
        int sx = Integer.parseInt(st.nextToken());
        int sy = Integer.parseInt(st.nextToken());
        int tx = Integer.parseInt(st.nextToken());
        int ty = Integer.parseInt(st.nextToken());
        
        String[] grid = new String[n];
        for (int i = 0; i < n; i++)
            grid[i] = br.readLine();
        
        System.out.println(gridBFS(grid, sx, sy, tx, ty));
    }
}`,
    },
  ]}
  cases={[
    {
      label: "기본",
      input: `5 5 0 0 4 4
.....
.###.
.....
.###.
.....`,
      output: `8`,
    },
    {
      label: "경로 없음",
      input: `3 3 0 0 2 2
...
.#.
...`,
      output: `4`,
    },
  ]}
/>

## 복잡도

| 항목 | 값 |
|:---|:---|
| **시간 (BFS)** | O(N × M) |
| **시간 (DFS)** | O(N × M) |
| **공간** | O(N × M) |
| **간선 수** | 최대 4NM (4방향) 또는 8NM (8방향) |

각 칸 최대 1회 방문. 인접 칸 탐색 O(1).

## 변형 / 활용

### 1. 최단 경로 (BFS)

가중치 없는 격자 최단 경로. 미로 탈출.

```cpp
int shortest_path = grid_bfs(grid, sx, sy, tx, ty);
```

### 2. 섬 개수 / 연결 영역 (DFS)

1 로 연결된 영역 개수.

```cpp
int count_islands(vector<vector<int>>& grid) {
    int cnt = 0;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            if (grid[i][j] == 1 && !visited[i][j]) {
                dfs(i, j);
                cnt++;
            }
        }
    }
    return cnt;
}
```

### 3. 다익스트라 (가중치 격자)

각 칸에 비용. 우선순위 큐 사용.

```cpp
priority_queue<tuple<int, int, int>> pq;  // (cost, x, y)
pq.push({0, sx, sy});
dist[sx][sy] = 0;

while (!pq.empty()) {
    auto [cost, x, y] = pq.top(); pq.pop();
    if (cost > dist[x][y]) continue;
    for (auto [dx, dy] : dirs) {
        int nx = x + dx, ny = y + dy, ncost = cost + grid[nx][ny];
        if (valid(nx, ny) && ncost < dist[nx][ny]) {
            dist[nx][ny] = ncost;
            pq.push({ncost, nx, ny});
        }
    }
}
```

### 4. 0-1 BFS (벽 부수기)

일반 이동 (비용 0), 벽 부수기 (비용 1). deque 사용.

```cpp
deque<pair<int, int>> dq;
dq.push_back({sx, sy});
dist[sx][sy] = 0;

while (!dq.empty()) {
    auto [x, y] = dq.front(); dq.pop_front();
    for (auto [dx, dy] : dirs) {
        int nx = x + dx, ny = y + dy;
        int w = (grid[nx][ny] == '#' ? 1 : 0);
        if (valid(nx, ny) && dist[x][y] + w < dist[nx][ny]) {
            dist[nx][ny] = dist[x][y] + w;
            if (w == 0) dq.push_front({nx, ny});
            else dq.push_back({nx, ny});
        }
    }
}
```

### 5. 8방향 이동

대각선 포함. 체스 킹, 지뢰 찾기.

```cpp
int dx[] = {-1, -1, -1,  0, 0,  1, 1, 1};
int dy[] = {-1,  0,  1, -1, 1, -1, 0, 1};
```

### 6. 다중 출발점 BFS

여러 시작점 동시 탐색. 토마토 문제.

```cpp
queue<pair<int, int>> q;
for (int i = 0; i < n; i++) {
    for (int j = 0; j < m; j++) {
        if (grid[i][j] == 1) {  // 초기 익은 토마토
            q.push({i, j});
            dist[i][j] = 0;
        }
    }
}
// BFS 진행
```

## 함정

### 1. 경계 체크 누락

`nx >= 0 && nx < n && ny >= 0 && ny < m` 빼먹으면 segfault. 함수로 분리 권장.

```cpp
bool valid(int x, int y) {
    return x >= 0 && x < n && y >= 0 && y < m && grid[x][y] != '#';
}
```

### 2. 방문 체크 타이밍

BFS 에서 큐에 넣을 때 즉시 `visited[nx][ny] = true`. pop 할 때 체크하면 중복 삽입.

```cpp
// 잘못 (중복)
if (!visited[nx][ny]) q.push({nx, ny});

// 올바름
if (!visited[nx][ny]) {
    visited[nx][ny] = true;
    q.push({nx, ny});
}
```

### 3. 좌표 순서 (x, y) vs (row, col)

`grid[x][y]` 가 `grid[row][col]` 인지 확인. x=행, y=열 또는 반대. 일관성 유지.

### 4. 대각선 이동 시 코너 컷

8방향 이동 시, 대각선으로 벽 사이를 뚫고 가는지 체크.

```cpp
// 대각선 (x+1, y+1) 이동 시, (x+1, y) 와 (x, y+1) 이 벽이면 막기
if (grid[x+1][y] == '#' && grid[x][y+1] == '#')
    continue;  // 코너 컷 방지
```

### 5. 거리 초기화

`dist[x][y] = -1` (미방문) vs `dist[x][y] = INF` (최단 거리). BFS 는 -1, Dijkstra 는 INF.

## BOJ 연습 문제

| 번호 | 제목 | 정답률 | 링크 |
|:---|:---|---:|:---|
| BOJ 1012 | 유기농 배추 | 45.3% | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/1000-1099/1012) |
| BOJ 2178 | 미로 탐색 | 42.1% | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/2100-2199/2178) |
| BOJ 2206 | 벽 부수고 이동하기 | 24.8% | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/2200-2299/2206) |
| BOJ 2667 | 단지번호붙이기 | 44.5% | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/2600-2699/2667) |
| BOJ 7576 | 토마토 | 38.9% | [kokoa-lab](https://github.com/kokoa-lab/boj-problems/tree/main/organize_problems/7500-7599/7576) |

## 참고

- [[BFS|너비 우선 탐색]]
- [[DFS|깊이 우선 탐색]]
- [[0-1 BFS|0-1 BFS]]
- [[Flood Fill|플러드 필]]
- [[Dijkstra|다익스트라 알고리즘]]
