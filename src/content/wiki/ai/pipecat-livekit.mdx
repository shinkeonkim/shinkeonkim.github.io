---
title: "[Voice AI] Pipecat vs LiveKit Agents: 프레임워크 비교"
aliases:
  - "Pipecat"
  - "LiveKit Agents"
  - "Vapi"
  - "Retell"
  - "voice agent framework"
  - "voice pipeline"
  - "Frame processor"
  - "build vs buy"
tags: [pipecat, livekit, voice-framework, agent, sfu]
category: ai
updated: 2026-06-25
references:
  - title: "Pipecat"
    url: "https://docs.pipecat.ai/"
  - title: "LiveKit Agents"
    url: "https://docs.livekit.io/agents/"
  - title: "Vapi"
    url: "https://vapi.ai/"
---

## 정의

2026 시점 *voice agent 프레임워크* 4가지 선택지:

| | Pipecat | LiveKit Agents | Vapi / Retell | 자체 구축 |
|---|---|---|---|---|
| 카테고리 | OSS framework | OSS SFU + framework | Managed | 직접 |
| 운영 | self-host | self-host 또는 LiveKit Cloud | 호스팅 | self |
| 학습 곡선 | 중간 | 중간 | *낮음* | 높음 |
| 비용 | 인프라만 | 인프라만 (LK Cloud 옵션) | 비쌈 ($0.13-0.30/min) | 인프라 |
| 유연성 | *높음* | 높음 | *낮음* (블랙박스) | *최고* |
| 시간 | 1-2주 | 1-2주 | 1-2일 | 1-3개월 |

## Pipecat

```mermaid
flowchart LR
    Frame[Frame] --> P1["Input Processor (mic)"]
    P1 --> P2[VAD Processor]
    P2 --> P3[STT Processor]
    P3 --> P4[LLM Processor]
    P4 --> P5[Aggregator]
    P5 --> P6[TTS Processor]
    P6 --> P7["Output Processor (speaker)"]
```

> *Frame 이 processor 파이프라인을 흐름*. Linux pipe 의 기능적 추상. 컴포넌트 *한 줄 교체*.

```python
from pipecat.pipeline.pipeline import Pipeline
from pipecat.services.deepgram import DeepgramSTTService
from pipecat.services.openai import OpenAILLMService
from pipecat.services.cartesia import CartesiaTTSService
from pipecat.transports.services.daily import DailyTransport
from pipecat.vad.silero import SileroVADAnalyzer

transport = DailyTransport(room_url, token, "Bot", DailyParams(
    audio_in_enabled=True,
    audio_out_enabled=True,
    vad_analyzer=SileroVADAnalyzer(),
))

pipeline = Pipeline([
    transport.input(),
    DeepgramSTTService(api_key="..."),
    OpenAILLMService(api_key="...", model="gpt-4o-mini"),
    CartesiaTTSService(api_key="...", voice_id="..."),
    transport.output(),
])

runner = PipelineRunner()
await runner.run(PipelineTask(pipeline))
```

### Pipecat 의 Frame 종류

| Frame | 의미 |
|---|---|
| `AudioRawFrame` | 오디오 청크 |
| `TextFrame` | 텍스트 |
| `TranscriptionFrame` | 전사 |
| `LLMResponseStartFrame` / `EndFrame` | LLM 응답 경계 |
| `TTSStartFrame` / `EndFrame` | TTS 시작/종료 |
| `UserStartedSpeakingFrame` | 사용자 발화 시작 |
| `UserStoppedSpeakingFrame` | 종료 |
| `InterruptionFrame` | 끼어들기 |

> *Frame 종류 추가 = 새 processor 추가*. 확장이 깔끔.

## LiveKit Agents

```mermaid
flowchart TB
    User1["사용자 1"] -->|WebRTC| SFU[LiveKit SFU]
    User2["사용자 2"] -->|WebRTC| SFU
    SFU -->|Room API| Agent["Voice Agent<br/>(headless participant)"]
    Agent --> STT
    Agent --> LLM
    Agent --> TTS
```

> *LiveKit SFU* 가 *WebRTC + room* 인프라. *Agent 가 room 에 headless participant 로 합류* → 다중 사용자 회의 / 룸 기반 voice agent 자연스러움.

```python
from livekit.agents import VoiceAssistant, AutoSubscribe, JobContext, WorkerOptions, cli
from livekit.agents.llm import ChatContext
from livekit.plugins import openai, silero, deepgram, cartesia

async def entrypoint(ctx: JobContext):
    await ctx.connect(auto_subscribe=AutoSubscribe.AUDIO_ONLY)

    initial_ctx = ChatContext().append(
        role="system",
        text="당신은 친절한 한국어 음성 어시스턴트입니다. 짧게 답하세요."
    )

    assistant = VoiceAssistant(
        vad=silero.VAD.load(),
        stt=deepgram.STT(language="ko"),
        llm=openai.LLM(model="gpt-4o-mini"),
        tts=cartesia.TTS(voice="...", language="ko"),
        chat_ctx=initial_ctx,
        min_endpointing_delay=0.5,
        allow_interruptions=True,
    )

    assistant.start(ctx.room)
    await assistant.aclose()
```

## Pipecat vs LiveKit Agents 결정

```mermaid
flowchart TD
    Q1{시나리오}
    Q1 -->|1:1 콜봇, 전화| Pipecat
    Q1 -->|다중 사용자 방, WebRTC 우선| LK[LiveKit Agents]
    Q1 -->|화상 회의 + AI 참여자| LK
    Q1 -->|복잡한 frame 변환| Pipecat
    Q1 -->|이미 LiveKit SFU 운영| LK
```

| | Pipecat | LiveKit Agents |
|---|---|---|
| Transport | Daily, Twilio, Plivo, FastAPI 등 다양 | LiveKit SFU 중심 |
| 추상 단위 | Frame + Processor | Function 기반 |
| 회의 / 룸 | 단순 | *강력* (Multi-participant) |
| Python | *우선* | Python + Node + Go |
| 학습 | 약간 가파름 | 친절한 docs |

## Managed (Vapi, Retell)

```mermaid
flowchart LR
    You[당신] -->|시스템 프롬프트만| Vapi[Vapi / Retell]
    Vapi -->|호스팅 파이프라인| Tel[Twilio 전화]
    Vapi -->|기본 STT/LLM/TTS| Pipeline
```

| | 장점 | 단점 |
|---|---|---|
| Vapi | 5분 만에 PoC | *블랙박스*, $0.13-0.30/min |
| Retell | 빠른 시작, 음성 좋음 | 동일, *latency 가시성 낮음* |

## Build vs Buy 기준 (Hamming AI 가이드)

```mermaid
flowchart TD
    Q{기준}
    Q -->|"월 < 10K 분"| Buy[Vapi / Retell]
    Q -->|"월 10-50K 분"| Mid["Pipecat / LiveKit (자체)"]
    Q -->|"월 > 50K 분"| Build[자체 + 깊은 최적화]
    Q -->|"엄격한 컴플라이언스 (HIPAA, FINRA)"| Build
    Q -->|"500ms 미만 지연 필수"| Build
    Q -->|"기존 system 깊은 통합"| Build
    Build --> Save["최대 80% 비용 절감 가능"]
```

> [!IMPORTANT]
> *PoC = Vapi/Retell*. *확장 시 → Pipecat/LiveKit 마이그레이션* 흐름이 일반.

## 복원력 설계

```mermaid
flowchart TB
    Q[복원력 요소]
    Q --> Fail[Independent failure points: STT, LLM, TTS]
    Q --> Strat[전략]
    Strat --> Retry[Retry + backoff]
    Strat --> Fallback["Fallback (다른 vendor)"]
    Strat --> Graceful["Graceful: #quot;잠시 후 다시#quot;"]
    Strat --> Health["Health check + circuit breaker"]
```

각 단계 독립 실패 → 우아하게 처리. 자세한 건 [[retry-with-backoff]] / [[circuit-breaker]].

## 흔한 함정

> [!WARNING]
> 1. **Managed 만 사용** = 비용 폭증 + 가시성 0. 1000분+ 면 self-host.
> 2. **Pipecat 의 Frame 무시** = 직접 콜백으로 짜다가 *경합 / 순서 문제*. Frame 모델 활용.
> 3. **LiveKit 의 transcription frame** = STT 결과가 *partial vs final* 분리 안 보임. 명시 처리.
> 4. **자체 구축 시 turn detection** = VAD 만으로 한계. 시맨틱 모델 필요. Pipecat / LK 가 내장한 이유.

## 레이턴시 구성 요소

voice agent 의 E2E 레이턴시는 각 단계가 직렬로 합산된다:

| 단계 | 일반적 범위 | 메모 |
|:---|:---|:---|
| VAD 종료 감지 | 200-500ms | 너무 빠르면 발화 중 끊김 |
| STT 첫 응답 | 300-600ms | streaming 시 단축됨 |
| LLM 첫 토큰 | 500-2000ms | 모델 크기, 네트워크 의존 |
| TTS 첫 청크 | 200-500ms | streaming TTS 시 단축됨 |
| 버퍼 / 전송 지연 | 50-200ms | WebRTC jitter buffer |
| **합계 최적화 전** | **1.2-3.8초** | - |

```mermaid
flowchart LR
    VAD["VAD 종료 감지"] --> STT["STT 인식"]
    STT --> LLM["LLM 첫 토큰"]
    LLM -->|"streaming 출력"| TTS["TTS 합성"]
    TTS --> Out["사용자 출력"]
```

LLM 첫 토큰 도착 즉시 TTS streaming 을 시작하면 LLM 생성 + TTS 합성이 병렬로 겹쳐 전체 체감 레이턴시가 줄어든다.

## 레이턴시 최적화 전략

1. **Streaming 파이프라인**: LLM 토큰 스트림 + TTS 동시 시작. Pipecat `LLMResponseAggregatorService` + `TTSService` 직결 시 자동 streaming.
2. **VAD 임계값 튜닝**: 민감도 높으면 발화 중 끊김, 둔감하면 레이턴시 증가. `threshold=0.5`, `min_silence_duration_ms=300` 이 출발점.
3. **모델 선택**: 짧은 응답은 GPT-4o-mini, 복잡한 추론만 GPT-4o. 한국어 TTS 는 Cartesia 가 ElevenLabs 보다 레이턴시 낮은 경우 많음.
4. **지역 배포**: STT / TTS API 와 서버 리전을 맞춤. 한국 사용자면 `ap-northeast-2` 우선.
5. **WebRTC 우선**: WebSocket 보다 P2P 최적 라우팅. LiveKit SFU 가 이를 담당.
6. **워밍업 요청**: 첫 발화 전 LLM / TTS 서비스에 더미 요청해 cold start 제거.

## 스택 선택 체크리스트

| 체크포인트 | Vapi / Retell | Pipecat | LiveKit Agents |
|:---|:---:|:---:|:---:|
| 1주 내 PoC | ✅ | 보통 | 보통 |
| 다중 참여자 룸 | 제한적 | 제한적 | ✅ |
| 커스텀 VAD / STT | 제한적 | ✅ | ✅ |
| 자체 인프라 운영 | 불가 | ✅ | ✅ |
| 비용 예측 가능성 | 선불 요금 | 인프라만 | 인프라만 |
| HIPAA / 컴플라이언스 | 미확인 | self-host 가능 | self-host 가능 |
| Python 생태계 우선 | - | ✅ | 보통 |
| Node / Go 지원 | - | 제한적 | ✅ |

## 프로덕션 운영 팁

> [!NOTE]
> Pipecat / LiveKit 모두 asyncio 기반. 블로킹 I/O 를 asyncio loop 안에서 직접 호출하면 전체 파이프라인이 멈춘다. `asyncio.to_thread` 또는 전용 executor 로 분리.

- **헬스 체크**: `/health` 엔드포인트 + LiveKit room 상태 폴링. 에이전트가 죽으면 참여자는 침묵만 들음.
- **그레이스풀 종료**: `SIGTERM` 수신 시 현재 발화 완료 후 종료. Pipecat `PipelineRunner.stop_when_done()` 활용.
- **분산 추적**: 각 Frame 에 `call_id`, `turn_id` 메타데이터 부착해 로그 연결.
- **요금 폭발 방지**: 무응답 30초 후 자동 종료. Vapi / Retell idle timeout 설정 확인.
- **에코 처리**: AEC (Acoustic Echo Cancellation) 는 transport layer 담당. Daily.co 는 내장, 직접 WebRTC 구현 시 별도 처리 필요.

## 관련 위키

- [[voice-agent-architecture]]
- [[vad-silero]]
- [[turn-detection-barge-in]]
- [[webrtc]]
- [[circuit-breaker]]
