---
import UrlPreview from './UrlPreview.astro';
import type { ExternalProfile } from '../lib/external-profiles';

interface Props {
  profile: ExternalProfile;
}

const { profile } = Astro.props as Props;
---

<UrlPreview url={profile.url} />
